---
import type { StatusBucket } from "../lib/tracker-data";

interface Props {
  bucket: StatusBucket;
  label?: string;
}

const { bucket, label } = Astro.props;
const display = label ?? bucket.replace(/_/g, " ");
---

<span class={`status-badge status-badge--${bucket}`}>{display}</span>
