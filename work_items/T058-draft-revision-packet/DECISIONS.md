# T058 — Decisions

## Metadata-only draft edits

T058 mutates only conservative summary and review-tracking fields on the T056 draft. No full legal text, no generated candidate body content, and no source verification claims.

## One revision record

Only `T058-001` is registered in this pilot. Additional revisions require a new Control Tower task.

## Draft schema extension

`draft-regulatory-update.schema.json` gains `review_status`, `latest_revision_id`, `latest_review_decision_id`, and confirmation flags required for revised drafts. `status` remains `draft_manual_review`.

## No network

T058 does not rerun T055 or any live fetch. Lineage references are registry metadata only.

## Generated output local only

`generated/draft-revisions/T058-001.json` is gitignored and not committed.
