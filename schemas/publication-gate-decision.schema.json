{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://caesar.no/schemas/caesar-ai-regulation-watch/publication-gate-decision.schema.json",
  "title": "Caesar AI Regulation Watch — Publication gate decision",
  "description": "Internal publication gate decision record. Not publication. Staging preview only when approved.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "publication_gate_decisions_id",
    "generated_at",
    "product_version",
    "legal_safe_note",
    "no_live_collection",
    "no_scheduled_monitoring",
    "decisions"
  ],
  "properties": {
    "publication_gate_decisions_id": {
      "type": "string",
      "pattern": "^publication-gate-decisions-[0-9]{4}-[0-9]{2}-[0-9]{2}-v[0-9]{3}$"
    },
    "generated_at": { "type": "string", "format": "date" },
    "product_version": { "type": "string", "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$" },
    "legal_safe_note": { "type": "string", "minLength": 1 },
    "no_live_collection": { "type": "boolean", "const": true },
    "no_scheduled_monitoring": { "type": "boolean", "const": true },
    "decisions": {
      "type": "array",
      "minItems": 1,
      "items": { "$ref": "#/$defs/decisionRecord" }
    }
  },
  "$defs": {
    "gateState": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "verified_on_source",
        "client_use_allowed",
        "client_evidence_allowed",
        "final_evidence_allowed",
        "legal_change_claimed"
      ],
      "properties": {
        "verified_on_source": { "type": "boolean", "const": false },
        "client_use_allowed": { "type": "boolean", "const": false },
        "client_evidence_allowed": { "type": "boolean", "const": false },
        "final_evidence_allowed": { "type": "boolean", "const": false },
        "legal_change_claimed": { "type": "boolean", "const": false }
      }
    },
    "safetyFlags": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "publication_allowed",
        "public_export_allowed",
        "publication_staging_allowed",
        "evidence_export_allowed",
        "client_use_allowed",
        "metadata_only",
        "stores_full_text",
        "requires_staging_preview_before_publication",
        "requires_separate_public_export_approval",
        "requires_separate_client_evidence_approval"
      ],
      "properties": {
        "publication_allowed": { "type": "boolean", "const": false },
        "public_export_allowed": { "type": "boolean", "const": false },
        "publication_staging_allowed": { "type": "boolean" },
        "evidence_export_allowed": { "type": "boolean", "const": false },
        "client_use_allowed": { "type": "boolean", "const": false },
        "metadata_only": { "type": "boolean", "const": true },
        "stores_full_text": { "type": "boolean", "const": false },
        "requires_staging_preview_before_publication": { "type": "boolean", "const": true },
        "requires_separate_public_export_approval": { "type": "boolean", "const": true },
        "requires_separate_client_evidence_approval": { "type": "boolean", "const": true }
      }
    },
    "decisionScope": {
      "type": "string",
      "const": "internal_publication_gate_decision_only"
    },
    "decisionStatus": {
      "type": "string",
      "enum": ["recorded", "superseded"]
    },
    "decisionValue": {
      "type": "string",
      "enum": ["approve_for_publication_staging", "request_changes", "reject"]
    },
    "decisionRecord": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "decision_id",
        "packet_id",
        "draft_update_id",
        "draft_update_path",
        "publication_gate_packet_id",
        "final_reviewer_recheck_id",
        "decision_scope",
        "decision_status",
        "decision",
        "decision_summary",
        "staging_limitations",
        "blockers_remaining",
        "next_required_step",
        "decided_by_role",
        "decided_at",
        "created_at",
        "updated_at",
        "gates",
        "safety"
      ],
      "properties": {
        "decision_id": { "type": "string", "pattern": "^T[0-9]{3}-[0-9]{3}$" },
        "packet_id": { "type": "string", "pattern": "^T[0-9]{3}-[0-9]{3}$" },
        "draft_update_id": { "type": "string", "minLength": 1 },
        "draft_update_path": { "type": "string", "pattern": "^data/regulatory-updates/drafts/" },
        "publication_gate_packet_id": { "type": "string", "pattern": "^T[0-9]{3}-[0-9]{3}$" },
        "final_reviewer_recheck_id": { "type": "string", "pattern": "^T[0-9]{3}-[0-9]{3}$" },
        "decision_scope": { "$ref": "#/$defs/decisionScope" },
        "decision_status": { "$ref": "#/$defs/decisionStatus" },
        "decision": { "$ref": "#/$defs/decisionValue" },
        "decision_summary": { "type": "string", "maxLength": 1000 },
        "staging_limitations": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "string", "minLength": 1 }
        },
        "blockers_remaining": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "string", "minLength": 1 }
        },
        "next_required_step": { "type": "string", "minLength": 1 },
        "decided_by_role": { "type": "string", "minLength": 1 },
        "decided_at": { "type": "string", "format": "date-time" },
        "created_at": { "type": "string", "format": "date" },
        "updated_at": { "type": "string", "format": "date" },
        "gates": { "$ref": "#/$defs/gateState" },
        "safety": { "$ref": "#/$defs/safetyFlags" }
      }
    }
  }
}
