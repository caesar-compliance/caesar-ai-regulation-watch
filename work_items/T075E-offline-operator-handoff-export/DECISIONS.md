# T075E — Decisions

## Handoff export shape

- Single JSON export aggregates sources, candidates, and decision packets with blocked reasons and cannot-claim-yet lists.
- Deterministic `generated_at` (`2026-05-21T14:00:00.000Z`) matches prior T075C/T075D fixture timestamp pattern.

## Markdown report location

- `public/reports/source-pilot-operator-handoff.md` — served as static asset alongside JSON exports; generated by the same build script.

## Runtime status

- `source_pilot_operator_handoff_ready` — indicates offline handoff layer is buildable; does not claim Supabase connected, monitoring active, or publication ready.

## UI

- Dedicated `/source-pilot/operator-handoff/` page with summary tiles, checklist, cannot-claim-yet, and next setup sections.
- Download links added to existing source pilot pages and automation page.
