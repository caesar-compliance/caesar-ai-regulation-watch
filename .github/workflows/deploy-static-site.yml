# Manual GitHub Pages deploy — pilot static site (v0.8.4)
# Does NOT run on push. Does NOT use secrets. Does NOT configure custom domain.
# Requires: Repository Settings → Pages → Build and deployment → GitHub Actions.

name: Deploy static site

on:
  workflow_dispatch:
    inputs:
      confirm_disclaimers:
        description: "Type DEPLOY to confirm pilot disclaimers and review gates were read"
        required: true
        type: string

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    if: inputs.confirm_disclaimers == 'DEPLOY'
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Generate evidence export candidates
        run: npm run generate:evidence-candidates

      - name: Validate data
        run: npm run validate:data

      - name: Generate static exports
        run: npm run generate:exports

      - name: Build site for GitHub Pages
        env:
          ASTRO_BASE_PATH: /caesar-ai-regulation-watch/
          ASTRO_SITE: https://caesar-compliance.github.io
        run: npm run build:pages

      - name: Verify dist output
        env:
          ASTRO_BASE_PATH: /caesar-ai-regulation-watch/
        run: npm run verify:dist

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

  gate-failed:
    if: inputs.confirm_disclaimers != 'DEPLOY'
    runs-on: ubuntu-latest
    steps:
      - name: Abort — confirmation required
        run: |
          echo "Deploy aborted. Re-run workflow and set confirm_disclaimers to exactly: DEPLOY"
          echo "See docs/PUBLIC_RELEASE_CHECKLIST.md and docs/STATIC_DEPLOYMENT_ARCHITECTURE.md"
          exit 1
