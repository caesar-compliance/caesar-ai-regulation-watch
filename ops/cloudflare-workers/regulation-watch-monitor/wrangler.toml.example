name = "regulation-watch-monitor"
main = "src/index.ts"
compatibility_date = "2024-11-01"

# T073: no scheduled triggers. Enable only after explicit approval.
# [triggers]
# crons = []

[vars]
RUNTIME_ID = "regulation-watch-runtime-v1"
WORKER_NAME = "regulation-watch-monitor-dev"
RUNTIME_ENV = "dev"

# Secrets (set via wrangler secret put — never commit):
# RUN_TOKEN
# SUPABASE_URL
# SUPABASE_SERVICE_ROLE_KEY
