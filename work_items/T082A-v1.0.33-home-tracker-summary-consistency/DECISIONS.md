# T082A — Decisions

- Keep package version **1.0.33** — content/consistency hotfix only.
- Homepage uses `regulation-review-queue` summary (20 candidates), not legacy registry review queue.
- Tracker places **T082 operator review pipeline** section before **T080 coverage dashboard**.
- Retain T080 coverage stats as secondary section on both routes.
- Smoke and route validators require T082 markers on `/` and `/tracker/`.
