{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://caesar.no/schemas/caesar-ai-regulation-watch/guidance.schema.json",
  "title": "Caesar AI Regulation Watch — Guidance record (sample)",
  "description": "Manual sample guidance record for data-model testing. Not legal advice.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "record_id",
    "record_type",
    "title",
    "jurisdiction_id",
    "source_id",
    "guidance_status",
    "official_url",
    "summary_for_review",
    "affected_topics",
    "record_origin",
    "review_status",
    "legal_safe_note"
  ],
  "properties": {
    "record_id": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9_-]*$"
    },
    "record_type": {
      "type": "string",
      "const": "guidance"
    },
    "title": {
      "type": "string",
      "minLength": 1
    },
    "jurisdiction_id": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9_-]*$"
    },
    "source_id": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9_-]*$"
    },
    "guidance_status": {
      "type": "string",
      "enum": [
        "proposed",
        "adopted",
        "in_force",
        "guidance",
        "voluntary",
        "withdrawn",
        "monitoring",
        "implementation_update",
        "unknown"
      ]
    },
    "record_origin": {
      "type": "string",
      "enum": [
        "official_source_registry",
        "manual_sample",
        "future_watcher_output"
      ]
    },
    "official_url": {
      "type": "string",
      "format": "uri"
    },
    "summary_for_review": {
      "type": "string",
      "minLength": 1
    },
    "affected_topics": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "minLength": 1 },
      "uniqueItems": true
    },
    "review_status": {
      "type": "string",
      "enum": [
        "draft",
        "pending_review",
        "reviewed",
        "needs_update",
        "rejected_for_client_use",
        "archived"
      ]
    },
    "legal_safe_note": {
      "type": "string",
      "minLength": 1
    }
  }
}
